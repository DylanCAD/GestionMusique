CREATE TABLE Personnel(
   numPersonnel VARCHAR(50),
   nom VARCHAR(50),
   grade VARCHAR(50),
   PRIMARY KEY(numPersonnel)
);

CREATE TABLE Stage(
   numStage VARCHAR(50),
   titre VARCHAR(50),
   PRIMARY KEY(numStage)
);

CREATE TABLE Stagiaire(
   numPersonnelStagiaire VARCHAR(50),
   numStageStagiaire VARCHAR(50),
   présent VARCHAR(50),
   PRIMARY KEY(numPersonnelStagiaire, numStageStagiaire),
   FOREIGN KEY(numPersonnelStagiaire) REFERENCES Personnel(numPersonnel),
   FOREIGN KEY(numStageStagiaire) REFERENCES Stage(numStage)
);

CREATE TABLE formateur(
   numPersonnelFormateur VARCHAR(50),
   numStageFormateur VARCHAR(50),
   PRIMARY KEY(numPersonnelFormateur, numStageFormateur),
   FOREIGN KEY(numPersonnelFormateur) REFERENCES Personnel(numPersonnel),
   FOREIGN KEY(numStageFormateur) REFERENCES Stage(numStage)
);
CADOTDY_Triggers